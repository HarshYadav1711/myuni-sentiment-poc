Place optional client-demo media here:

  positive_image.jpg   (or .jpeg / .png)
  negative_image.jpg   (or .jpeg / .png)
  demo_video.mp4       (or .mov / .webm)

The Streamlit demo discovers these automatically when present.
No model predictions are fabricated for image/video assets.

Note: the current positive_image.jpg is NOT a reliable client demo of
a positive SigLIP 2 result (live scoring was negative). Replace it
with a clearly-positive sample before client demonstration. Negative
and video assets may remain. A short spoken audio sample is still needed
to exercise Faster-Whisper end-to-end.
